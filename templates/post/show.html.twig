{% extends 'base.html.twig' %}
{% block title %} Post - {{ post.title }} {% endblock %}
{% block body %}
    <h3>{{ post.title }}</h3>
    <div>{{ post.created|date('D-m-Y') }}</div>
    <div>{{ post.text }}</div>
    <div><a href="{{ path('app_post') }}">Back</a></div>
{% endblock %}